-content_for :header do
  = stylesheet_link_tag 'games_index.css'

.title
  ChessArabia
  
.menu
  [
  = link_to 'New Game', games_path, :method => "post"
  |
  = link_to 'Play Against Computer', play_with_computer_games_path, :method => "post"
  ]

.games
  - @games.each do |game|
    .game{ :id => "game_#{game.id}" }
      = link_to 'Visit Game', game_path(game.id), :class => "link"
